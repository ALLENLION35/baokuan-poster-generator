# 参与贡献

欢迎改进中文排版、主题、导出流程、文档与测试。提交前先搜索已有 Issue；涉及新输出模式、数据模型或新增依赖时，先用 Issue 说明使用场景和方案。

## 本地开发

准备 Node.js 22+、Python 3、Chromium 和中文字体。在仓库根目录运行：

```sh
npm ci
npx playwright install chromium
npm run check
npm test
```

若使用已有依赖或浏览器，可设置 `POSTER_NODE_MODULES` 和 `POSTER_BROWSER`，路径以本机实际位置为准。Python 可通过 `PYTHON` 指定。完整环境说明见 [README](README.md)。

工作源稿、素材和导出放在 `work/`、`outputs/`；这两个目录不进入版本控制。测试用虚构文案、几何 Logo 和本地生成素材，不提交真实客户资料或可用二维码。

## 修改与验证

- 修复缺陷时先写能复现问题的回归测试，再修改实现。执行相关测试与完整 `npm test`。
- 渲染改动需检查长图和组图，特别是文字裁切、资源内嵌、页数变化及覆盖回滚。
- 配色改动需同时检查深浅主题、亮色 Logo 与双色渐变；检查文字可读性，并保留品牌原色用途。
- 排版、主题或模板改动请提供修改前后样图，说明示例文案和素材来源。先确保信息完整、手机可读，再判断风格。
- 文档改动检查命令、相对链接和功能表述；不需要为纯文案改动新增测试。
- 新依赖应说明用途、替代方案、许可证与维护成本，不仅为了缩短几行代码引入框架。

## 提交 Pull Request

一个 PR 聚焦一个问题。说明触发条件、修改后的行为、验证结果，以及需要注意的兼容变化。不要把重新排版整个文件、生成的大图和功能修改混在同一个差异中。

代码遵循现有 CommonJS 与 Python 标准库风格；项目目前未配置单独的 lint 或类型检查器，不要将未执行的检查写成通过。涉及接口时同步 `references/` 与 [CHANGELOG](CHANGELOG.md)。

提交者应有权贡献代码与素材；新增第三方内容需注明来源和许可证。项目代码按 [MIT](LICENSE) 分发，依赖和字体仍遵循其各自许可证。欢迎使用 AI 辅助，但提交者需检查事实、代码和测试结果。

## 交流

用具体复现、截图与预期行为讨论问题，尊重不同使用场景。涉及潜在漏洞或敏感内容，请先阅读 [安全说明](SECURITY.md)，不要在公开 Issue 中附上真实凭据或客户数据。

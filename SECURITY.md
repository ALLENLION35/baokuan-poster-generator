# 安全与问题报告

## 使用边界

爆款海报生成器用于本地可信项目与经授权的素材，不是运行任意 HTML 的安全沙箱。渲染器拒绝脚本、嵌入文档、外部样式表和远程资源，并将支持的本地素材内嵌到导出 HTML。

`--asset-root` 扩展允许读取的素材目录，只指定当前工作需要的目录。导出的 HTML 包含素材本身，分享前检查是否适合公开。项目不上传素材或连接发布平台；所使用的 Agent 宿主或图像生成服务有各自的数据处理方式。

## 报告问题

普通运行错误请通过仓库 Issue 提供脱敏后的最小复现。潜在漏洞、凭据或私人数据不要贴在公开 Issue 中。

如果仓库的 Security 页面提供 **Report a vulnerability**，请使用私密报告入口。若没有该入口，可先提交不含利用细节或敏感附件的联系请求，由维护者提供私下沟通方式。项目未公布安全联系邮箱，不要向推测的邮箱发送敏感资料。

有用的信息包括：受影响版本、运行环境、预期与实际行为、最小复现及可能影响。请使用虚构资料复现，不需要访问他人文件或账号来证明问题。

## 维护范围

维护重点是最新的 1.x 版本；旧预览版本不单独承诺安全回补。修复和响应按维护者可用时间处理，本项目没有响应时限或长期支持承诺。
